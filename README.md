# 🧹 Windows Cleanup SwissKnife

A safe but powerful PowerShell-based cleanup utility for Windows developers and power users.

## ✨ Features
- Interactive prompts — choose exactly what to clean.
- Cleans npm, pip, conda, Docker, Flutter, Gradle, VSCode, node_modules.
- Multi-Python cache detection (auto-detects all python.exe installs).
- Safe LM Studio cleanup (keeps chat history + settings).
- Optional Windows Search reindex and temp folder cleanup.
- Optional browser cache cleaner (supports Chrome, Edge, Brave, Firefox, Tor, Mullvad, etc).
- Doesn’t touch credentials, saved passwords, or user projects.

## 🧰 Requirements
- Windows 10/11
- PowerShell 7+ (recommended)
- Admin privileges

## 🚀 Usage
1. Clone the repo:
   ```bash
   git clone https://github.com/<yourname>/Windows-Cleanup-SwissKnife.git
   cd Windows-Cleanup-SwissKnife
